---
name: speckit.specify
description: Use when the user asks to run the speckit.specify prompt or references speckit.specify tasks.
---

# speckit.specify Prompt

## Purpose
Route requests to the speckit.specify prompt in `.codex/prompts/speckit.specify.md`.

## Workflow
1. Open `.codex/prompts/speckit.specify.md`.
2. Follow the instructions in that prompt when responding to the user.
3. If the prompt is missing, tell the user the file is not present and ask whether to create it.
